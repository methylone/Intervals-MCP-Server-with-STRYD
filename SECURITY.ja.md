[English](SECURITY.md) | 日本語

# セキュリティとプライバシーモデル

このサーバは MCP クライアント（例: Claude Desktop）と Intervals.icu API をつなぐ薄いブリッジです。
30 秒で言うと: 通信先は **1 つのリモートホスト**（intervals.icu）のみ、書き込めるのは
**あなたの Intervals.icu のカレンダーイベントだけ**、API キーは **ログに出さない**、
**テレメトリは送りません**。以下に「何をする / しないか」を正確に記します。

## Claude Desktop の「未検証」警告について

`.mcpb` バンドルをインストールすると、Claude Desktop は拡張機能が **Anthropic に未検証** で
**あなたの PC のファイルを読み書きできる** と警告します。これはローカルにインストールされる
すべての拡張機能に共通する一般的な文言で、Node.js プロセスが*できること*を述べているだけで、
このサーバが実際に*すること*ではありません。警告自体は消せません（消えるのは Anthropic 公式
ディレクトリ掲載時のみ）。このサーバが実際に触れる範囲は以下に列挙してあり、ソースは AGPL で
監査可能です。

## 通信先

- **リモートホストは `https://intervals.icu/api/v1` の 1 つだけ。** コード内の外向き通信はこれのみ。
- **テレメトリ・解析・クラッシュレポートは一切なし。** 他のどこにも何も送信しません。
- `search_similar_activities` が返す天候 / 体感温度は、**Intervals.icu が既にアクティビティに
  保存している値**を読むだけで、サーバ自身が天候プロバイダを呼ぶことはありません。

## 読み取りと書き込み

**読み取り（Intervals.icu・読み取り専用）:** アクティビティ、ストリーム、ウェルネス、
アスリートサマリー、カレンダーイベント、HRV。

**Intervals.icu アカウントへの書き込みは「カレンダーイベント」のみ。** 書き込みを行うのは
ちょうど 4 つのツールで、対象は*計画カレンダーイベント*に限られます:

- `create_events`, `update_event`, `delete_event`, `delete_events`

アクティビティやウェルネスのデータを変更・削除することは**ありません**。カレンダーイベントの
削除は不可逆なので、AI は通常その前に確認します — どのツール呼び出しを承認するかはあなたの
管理下にあります（下記「チャットでの初回実行」参照）。

**ローカルへの書き込み:** ディスク上のストリームキャッシュのみ（次節）。

## Intervals.icu API キー

- **影響範囲（blast radius）:** Intervals.icu の API キーは**アカウントへの全アクセス権**
  （読み取り + 上記カレンダー書き込み）を持ちます。Intervals.icu にはスコープ付き / 読み取り専用
  キーの仕組みがないため、全権限の資格情報として扱ってください。
- **保管場所:** ソースからの利用では `.env`（gitignore 済み）の中だけ。`.mcpb` バンドルでは
  `sensitive` と宣言されるため、Claude Desktop は平文設定ファイルではなく **OS キーチェーン**に
  保管します。
- **ログには決して書き出しません。** キーは intervals.icu への HTTP `Authorization` ヘッダの中
  だけを通ります。
- **失効 / ローテーション**はいつでも Intervals.icu → *Settings → Developer* で可能。キーが
  露出した場合は直ちに実施してください。

## ディスク上のストリームキャッシュ

- **保存する内容:** 分析に使うアクティビティ別の時系列 — `time`、心拍、パワー、速度 / ペース、
  距離、勾配、ケイデンス。**GPS / 位置情報（`latlng`）や個人識別子は保存しません。**
  ストリームデータは不変なので TTL はありません。
- **場所:** `.mcpb` バンドルでは `~/.intervals-mcp-with-stryd/cache/streams`。ソースからは
  `<サーバ設置先>/cache/streams`（`CACHE_DIR` で上書き可）。
- **作成タイミング:** インストール時には何も書きません。初めてストリーム分析を実行したときに
  遅延作成されます。サイズはアクティビティあたり数 KB〜MB 程度。
- **無効化 / 削除:** `CACHE_ENABLED=false` で読み書きを一切しない、実行時に
  `set_cache_enabled` / `clear_cache` ツールを呼ぶ、または
  `rm -rf ~/.intervals-mcp-with-stryd/cache/streams`。

## ログ

- サーバは診断情報を **stderr** に書き、クライアントがそれを取り込みます。macOS の Claude Desktop
  では `~/Library/Logs/Claude/mcp*.log`。
- ログに含まれるのはツール名・リクエスト ID・所要時間・ステータスコードで、**API キーや
  athlete ID は含まれず**、**Intervals.icu のエラーレスポンス本文も残しません**（エラー本文は
  文脈として AI には渡しますが、ログには永続化しません）。これは `npm test` のたびに走る
  テストで担保しています。

## HTTP モードはインターネットに対して安全ではない

任意の Streamable HTTP トランスポート（`MCP_TRANSPORT=http`）はオプトインで、
**アプリケーション層の認証を持たず**、`0.0.0.0`（すべてのインターフェース）にバインドします。
そのポートに到達できる者は誰でも、あなたの資格情報で全ツールを呼べます。

- **デフォルトかつ推奨は `stdio`。** クライアントがローカルのサブプロセスとして起動し、
  ネットワーク上では何もリッスンしません。`.mcpb` インストールはこれを使います。
- HTTP が必要な場合（複数クライアント・常時稼働サーバ）は、**ネットワーク層**でアクセスを
  制限してください — [Tailscale](https://tailscale.com) のような VPN 内のみで実行するか、ポートを
  信頼できるホストに限定するファイアウォールの背後で実行します。
- **HTTP エンドポイントを公開インターネットに絶対に晒さないでください。** 認証・レート制限・
  ユーザごとの分離はありません。

## チャットでの初回実行

AI が初めてツールを使うとき、MCP クライアントが承認を求めます。呼び出しごと、または
セッション単位で承認できます。*読み取り*ツールの承認は AI にデータ取得を許すだけです。
アカウントへの変更が起きるのは 4 つのカレンダー書き込みツールなので、それらのプロンプトは
よく確認してください。

## ビルドの検証

- **ソースから再現可能:** `git clone` → `npm ci` → `npm run build` →
  `npx @anthropic-ai/mcpb pack` でバンドルを再現できます。npm 以外から何も取得しません。
- **チェックサム:** 各 GitHub Release は `.mcpb` の **SHA-256** を掲載します。ダウンロードした
  ファイルを `shasum -a 256 intervals-mcp-with-stryd.mcpb` で照合してください。

## 更新

- **`.mcpb`（Claude Desktop）:** 自動更新はありません — GitHub Releases から新しいバンドルを
  再ダウンロードして入れ直してください。
- **`npx` / npm:** `npx intervals-mcp-with-stryd@latest` が最新公開版を追従します。

## アンインストール

1. Claude Desktop で拡張機能を削除（Settings → Extensions）、または グローバル npm インストールの
   場合は `npm rm -g intervals-mcp-with-stryd`。
2. キャッシュ / 設定フォルダを削除: `rm -rf ~/.intervals-mcp-with-stryd`。
3. 必要に応じて Intervals.icu → *Settings → Developer* で API キーを失効。

## 対応プラットフォーム

**macOS** で開発・検証済み。コードはクロスプラットフォーム（Node ≥ 20.12）で Windows / Linux でも
動作する想定ですが、まだ常時テストはしていません — 他プラットフォームからのフィードバックや
Issue を歓迎します。

## 脆弱性の報告

セキュリティ上の問題は、このリポジトリの **GitHub Issues** から報告してください。問題と再現手順を
記載し、報告に実際の API キーや個人データを含めないでください。
