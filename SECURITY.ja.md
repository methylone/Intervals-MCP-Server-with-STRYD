[English](SECURITY.md) | 日本語

# セキュリティ

## トランスポート: HTTP モードはインターネットに対して安全ではない

Streamable HTTP トランスポート（`MCP_TRANSPORT=http`）は**アプリケーション層の認証を持たず**、
`0.0.0.0`（すべてのネットワークインターフェース）にバインドします。そのポートに到達できる者は誰でも、
あなたの Intervals.icu 認証情報を使ってすべてのツールを呼び出せます。

- **個人利用のデフォルトかつ推奨は `stdio`。** クライアントがサーバをローカルのサブプロセスとして
  起動し、ネットワーク上では何もリッスンしません。
- HTTP が必要な場合（複数クライアント、常時稼働サーバ）は、**ネットワーク層**でアクセスを制限して
  ください。[Tailscale](https://tailscale.com) のような VPN の内部でのみ実行するか、ポートを信頼できる
  ホストに限定するファイアウォールの背後で実行します。
- **HTTP エンドポイントを公開インターネットに絶対に公開しないでください。** 認証・レート制限・
  ユーザごとの分離はありません。

## 認証情報

- Intervals.icu の API key と athlete ID は **`.env` の中だけ**に置きます。`.env` は gitignore
  されています。決してコミットしないでください。
- `.env.example` にはプレースホルダのみが含まれます — `.env` にコピーし、実際の値はローカルで
  記入してください。
- クライアント設定を貼り付けるときや AI アシスタントにセットアップを依頼するときは、実際の
  API key を共有・ログ記録される会話に入れないようにしてください。
- もし key が露出した場合は、Intervals.icu（*Settings → Developer*）で**ローテーション**して
  ください。

## 脆弱性の報告

セキュリティ上の問題は、このリポジトリの **GitHub Issues** から報告してください。問題と再現手順を
記載してください。報告に実際の API key や個人データを含めないでください。
