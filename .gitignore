# Secrets / local env
.env
.env.*
!.env.example
intervals_api_keys.txt

# PII deny-list for the artifact/metadata guards (real name/email/home path).
# Never committed, never published — its contents are the literals we forbid.
.pii-forbidden

# Build output / deps / cache
build/
node_modules/
cache/

# MCPB bundle artifact (distributed as a GitHub Release asset, never committed)
*.mcpb

# OS / editor cruft
.DS_Store
.stfolder/

# Temporary verification scripts
tmp_*.ts

# Dev API smoke-test scratch (not part of the public package)
test-api.ts
