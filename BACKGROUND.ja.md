[English](BACKGROUND.md) | 日本語

# 経緯（Background）

このサーバがなぜ存在するのか、その背後にある原則についての短い非公式な記録です。
フォークする人向けの背景であり、仕様書ではありません。

## なぜ作ったか

個人ツールとして始まりました。開発者でもある持久系ランナーが、トレーニングデータを
「それらしく」ではなく「正しく」分析できる AI アシスタントを欲しがったのです。
言語モデルは物語化やパターン発見は得意ですが、トレーニング負荷の数値を手計算で
出させるのは信頼できません。そこで目標はこうなりました——AI には推論させ、
厳密な計算はすべて、AI が呼び出せる決定的なコードに移す。

## コアとなる原則：決定的なサーバと、分離されたナレッジ

設計を形づくった二つの考え方：

1. **決定的 vs. 確率的の分離。** 厳密でなければならないもの——Performance Management
   Chart の値、指数移動平均、デカップリング、派生栄養フィールド——は、モデルの頭の中
   ではなくテスト済みの TypeScript に置く。サーバが決定的な半分、AI クライアントが
   確率的な半分です。

2. **コード vs. 方法論の分離。** このサーバは「どう」トレーニングするかを一切知りません。
   トレーニング哲学、ゾーンモデル、プラン論理は完全に分離し、ユーザが自分で書いて
   AI クライアントに読み込ませるナレッジファイルに置きます。これによりコードは汎用で
   再利用可能なまま保たれ、各ユーザが自分の方法論を持ち込めます。公開リポジトリには
   そのための空のテンプレートだけを同梱しています——[`training-knowledge-template/`](training-knowledge-template/) 参照。

## どう育ったか

開発は段階的で、「use first, then build（まず使い、それから作る）」——インフラに
投資する前に、実利用を通じて必要性を検証する——という方針に導かれました。おおよその順：

- コアの Intervals.icu アクセス（アクティビティ、ウェルネス、イベント、アスリートサマリー）。
- 下肢負荷の Performance Management Chart をサーバ側で計算し、内蔵の負荷指標と並べて
  提供する Stryd パワー拡張。
- ストリーム単位の分析（スプリット、心拍デカップリング、地形を踏まえたペース）。
- トレーニングプランを投入するためのカレンダーイベントの作成・更新・削除。
- エネルギーアベイラビリティ分析のための派生栄養フィールド。

コア／拡張の分離により、汎用部分は再利用可能に、パワーメーター固有の部分は任意に
保たれました。小さな「ツールインベントリ」テストが、リファクタ中にツールが静かに
消える事故——実際に一度起き、見落としやすかった——を防いでいます。

## 開発プロセスについて

ビルドの多くは、AI を層状に使って行われました——設計とレビューに一つのモデル、
実装に別のモデルを使い、両者の間でハンドオフのメモを受け渡す。これはトレーニング
ツールであると同時に、AI 支援開発の実験でもあります。その詳細な履歴は私的な開発ログに
あり、この公開リポジトリはその結果のクリーンなスナップショットです。
